name: Docker Build 
description: 'Some test about docker'
inputs:
  machine-name:
    description: 'name for docker hub'
    default: 'mDemo'

runs:
  run: cd ./src/
  using: 'docker'
  image: 'Dockerfile'