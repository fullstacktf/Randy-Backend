name: Docker Build 
description: 'Some test about docker'
inputs:
  machine-name:
    description: 'name for docker hub'
    default: 'mDemo'

runs:
  using: 'docker'
  image: 'Dockerfile'
  env:
    GITHUB_WORKSPACE: /home/runner/work/Randy-Backend/Randy-Backend/src